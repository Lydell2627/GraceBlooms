# 🌸 Full-Stack Flower Shop App - Setup Guide

Welcome to your new Flower Shop Application! This project is built with the T3 Stack (Next.js, tRPC, Prisma, Tailwind CSS) and features real-time chat.

## 🚀 Quick Start
1. **Explore the Docs**:
   - `MANIFEST.md`: See what files are included.
   - `docs/getting-started.md`: Your 6-day build checklist.
   - `docs/project-setup.md`: Understanding the folder structure.

2. **Install Dependencies**:
   ```bash
   npm install
   ```

3. **Database Setup**:
   ```bash
   npx prisma db push
   ```

4. **Run Development Server**:
   ```bash
   npm run dev
   ```

5. **Run WebSocket Server (for Chat)**:
   ```bash
   npm run ws
   ```

## 📂 Key Directories
- `src/app`: Frontend pages (Next.js App Router)
- `src/server`: Backend API (tRPC routers)
- `prisma`: Database schema

## 🆘 Need Help?
Check `docs/quick-reference.md` for common issues and API endpoints.
